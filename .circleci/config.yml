version: 2.1

jobs:
  build:
    docker:
      - image: circleci/node:4.8.2
    steps:
      - checkout
      - run: npm install
      - run: npm run build -- retire
      - save_cache:
          key: v1-repo-{{ .Environment.CICRLE_SHA1 }}
          paths:
            - node_modules/

